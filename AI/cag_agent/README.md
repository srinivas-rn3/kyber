# LangGraph CAG Agent (AWS Bedrock + Claude 3 Haiku)

A simple Context-Aware Guardrails (CAG) agent built with:

- LangGraph (deterministic workflow)
- AWS Bedrock (Claude 3 Haiku)
- JSON rule engine

## Project Structure

